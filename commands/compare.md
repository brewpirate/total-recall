---
name: compare
description: Run cross-model comparison of trigger phrase generation (haiku vs sonnet vs opus)
---

Load and follow the total-recall:compare skill. Pass any user arguments as context — they specify which file or directory to compare.
